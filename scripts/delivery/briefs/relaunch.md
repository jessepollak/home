# Lane: resume issue #{{ISSUE_NUMBER}} — {{ISSUE_TITLE}}

Attempt {{ATTEMPT}}. A previous lane on branch `{{BRANCH}}` exited (timeout, crash, or stall) before opening a PR. The worktree `{{WORKTREE}}` keeps its commits and any uncommitted WIP: run `git status` and `git log --oneline origin/main..HEAD` first and continue from there. Do not start over unless the existing work is wrong.

## Issue

{{ISSUE_BODY}}

{{CONTRACT}}
