## Summary

<!-- What changed, why, and the issue. -->

Closes #

## Test plan

- [ ] `bun check` / CI green
- [ ] Smoke the changed surface (or N/A — say why)

## Operator action required

<!-- Optional. If this PR needs an operator action to work after merge, state exact environment variable names, non-secret setting values, and exact commands here (for example: a new environment variable, migration command, provider-dashboard change, or Vercel setting). Never include secret values, credentials, tokens, private keys, or customer data in the PR or ready comment. The ready comment must name the step; the PR stays `status:needs-jesse` until Jesse completes it, then the lane verifies the production path. Otherwise write N/A. -->

## Preview

<!-- User-visible changes only: include the Vercel Home preview link, at least one screenshot (~390px) or one short video from the current head, and concise agent-browser evidence: mode (factory fixture/operator), route, CSS-pixel viewport, and exercised path including recovery/Back. Attach every screenshot or clip retained as PR evidence directly to this description as a GitHub user-attachment, with a descriptive state/viewport label; do not commit media or attach only one representative from a larger retained set. This does not require creating a screenshot matrix. When Storybook review applies, optionally keep Before / Proposed / Implemented references here: Before capture + state/data/viewport; Proposed status (unreviewed or Jesse-reviewed), commit, immutable deployment-specific URL, direct manager/canvas links, selected capture, observable criteria, and the actual unmarked Jesse comment/review link when reviewed; Implemented current-head Home proof of the same component at matching state/data/viewport plus the agent-browser evidence. Include failure/recovery and reduced-motion browser/emulation evidence, and motion media, when applicable. State exact limitations. Refresh affected links, media, and Home evidence after UI changes; never call an unreviewed proposal approved. Keep all media here; do not add duplicate proof/approval fields or a manifest. See docs/browser-validation.md and docs/ui-pr-previews.md. Otherwise write N/A: docs-only / CI-only / pure server. -->
