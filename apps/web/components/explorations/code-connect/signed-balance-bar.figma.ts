// url=https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=293-5935
// source=apps/web/components/signed-balance-bar.tsx
// component=SignedBalanceBar
// The items axis describes breakdown data, not a SignedBalanceBar prop variant.
import figma from 'figma'
export default { example: figma.code`<SignedBalanceBar items={breakdown} selectedId={null} onSelect={() => {}} />`, imports: ['import { SignedBalanceBar } from "@/components/signed-balance-bar"'], id: 'signed-balance-bar', metadata: { nestable: true } }
