# Home-owned product pieces

Each Home-owned product behavior has its own file. Add a file per new piece rather than appending to the [UI-system guide](../../design-system.md#home-owned-product-pieces).
