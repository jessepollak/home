# Story inventories

Each story group has its own file in this directory. Add new groups as files rather than extending the shared UI-system guide. The built Storybook `index.json` and MCP `docs-list` are the current discoverability sources.
