# Historical PR preview assets

The live convention is [UI PR previews](../ui-pr-previews.md): the Vercel preview link plus one inline screenshot or short clip in the **GitHub PR description** (GitHub user-attachments only). Do not add new PNGs here.

`pr-3-home-shell/` (`home.png`, `account.png`) is a past example from [PR #3](https://github.com/jessepollak/home/pull/3). Leave those files in place.
