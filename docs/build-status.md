# Build status

Snapshot: September 8, 2026. The integrated local finance spike includes funding, USDC/ETH transfers, Morpho savings actions, email-controlled crypto trades, one supported Borrow market, valuation, durable operations, and the final presentation pass. It remains behind verified-account/provider gates. No live wallet signature, funded end-to-end flow, or deployment has been performed. Publication history is recorded in Git; local implementation acceptance is not production authorization.

Final pre-publication validation passed: **372 tests, one opt-in live test skipped**, lint, strict TypeScript, production build, seven production-component Chromium auth scenarios with mocked boundaries, and the real PostgreSQL money/attempt contract. Independent reviews covered the feature and runtime work; parent inspected the final dispatch-expiry and ABI-derived receipt nonce corrections. Mobile/desktop presentation checks passed with screenshots kept outside the repository. Anonymous browser checks verified that development document navigation on `127.0.0.1` redirects once to `localhost`; the canonical origin passes CDP CORS. These checks do not establish live authenticated session persistence.

| Feature | Owner / state | Built | Integrated | Reviewed / validated | Visible locally | Exact remaining gate |
| --- | --- | --- | --- | --- | --- | --- |
| Globe interaction and landing placement | Accepted prior closeout | Yes | `1c3fc5b` | Independent review OK; focused tests and prior localhost WebGL check | Yes | Physical-device/GPU coverage remains limited |
| Rotation-triggered country/currency popovers | Accepted local UI change (HOME-3) | Yes | Existing globe projection callback | Independent review OK; deterministic selection test, mobile fit and actual-landing rotation checks | Yes | Physical-device/GPU coverage remains limited |
| Shared compact finance rows | Accepted local UI change (HOME-4) | Yes | Activity, Home balances and Invest | Independent review OK; existing focused tests and 320px/desktop visual checks | Yes | Real-account visual feedback remains welcome |
| Email and Base Account authentication | Live refresh verification open (HOME-5) | Recovery fixes integrated | Yes | Mocked auth scenarios pass; canonical development-origin redirect verified in actual anonymous Chrome | Yes | Use `http://localhost:3000`; if authenticated refresh still fails there, capture the redacted diagnostic. Live email/Base acceptance remains open |
| Public landing and `/dashboard` routing | Accepted local recovery | Yes | Yes | Independent logout/routing review and deterministic composition checks pass | Yes | Real Next successful-login/logout acceptance remains user-controlled |
| USDC and native ETH balances | Integrated recovery | Yes | Dashboard composition | RPC/auth/race and refresh-trigger tests pass | Yes | Real authenticated read acceptance; managed RPC recommended for production |
| Authenticated Activity | Accepted local implementation | Yes | Strict paginated `/api/activity`, account client, and `ActivityPanel`; ordinary Home-action list hides pre-chain Rejected | Independent review and query/freshness/cancellation fixes validated; compact rows separately reviewed | Yes | Live scoped owned-account read; intentionally excludes native ETH and complete ERC-4337 history |
| Wallet transfer actions | Integrated local implementation | USDC/ETH send and receive; Send uses stepped MoneyModal (amount → address → confirm) over dimmed Home; amount step has asset picker, $10/$25/Max, and local↔native toggle | Durable owner-scoped operations plus receipt-driven refresh | Canonical plans, dispatch expiry, atomic claims, exact call/digest proof, ABI-correct event nonces, and the PostgreSQL money/attempt store contract validated; Send confirm is human product rows (no approval base units / expiresAt / chain 8453) | Yes | No live funded send or production approval; money actions require `DATABASE_URL` and `MONEY_ACTION_POSTGRES_CUTOVER=verified-empty`; signed trade payloads intentionally expire from process memory |
| Savings: public vault data | Accepted read-only source | Yes | Yes | Public Morpho evidence, strict rates and cache checks | Yes | Variable rates and liquidity are not guaranteed |
| Savings: authenticated positions | Integrated recovery | Yes | `/api/savings/positions` + consumer | Scoped fixtures, account switching, and private boundary tests pass | Yes | Real owned-position acceptance; null remains distinct from zero spendable balance |
| Savings deposit and withdrawal | Integrated local implementation | Dollar hero + vault cards; Deposit/Withdraw reuse shared MoneyModal (amount → confirm, no address) with locked USDC pill, Max, and local↔native toggle | Shared review, exact approval and user-controlled execution | Independent protocol review and focused/integrated checks passed | Yes | Live vault execution and funded-wallet acceptance not performed |
| Crypto trading | Integrated local implementation | Email-controlled smart-account Permit2 flow | Prepare, sign, finalize, review, claim and execute | Independent signature/owner/quote review and deterministic tests passed | Yes | Live CDP quote/signature/receipt acceptance open; stocks and external Base/passkey trading remain gated |
| Borrow | Integrated bounded spike | One cbBTC/USDC Morpho market | Collateral, borrow, repay/all and withdrawal UI | Reviewed math, batch simulation and finite-cap corrections validated | Yes | Compatible deployed accounts only; counterfactual simulation and live funded execution not accepted |
| Shared financial formatting | Accepted local implementation | Yes | Portfolio, Activity, Invest and Savings | Independent review and scientific-zero fix validated; exact transfer review remains unrounded | Yes | No live-provider price/position acceptance implied |
| Codex prices | Accepted prior schema fix | Yes | `GetPriceInput` adapter and Invest consumer | Exact-contract adapter tests and earlier live schema evidence | Yes | Freshness varies; production usage terms and user-facing acceptance remain separate |
| Country coverage | Accepted prior presentation work | 39 ISO countries / 19 currencies | Yes | Registry and selector tests | Yes | Presentation only; local funding is not enabled |
| Wallet & savings value and USD/local Cash buckets | Integrated local implementation | Yes | Separate `/api/portfolio/valuation?region=...`, account client, hook and Home composition | Exact-math, raw-price, FX, Token Balances directs + pinned Morpho convert RPC, owner/currency and USD/EUR/GLOBAL composition tests pass; presentation explicitly labels Borrow separately | Yes | Real authenticated Token Balances + dedicated `BASE_RPC_URL` vault convert smoke; bounded inventory is wallet assets plus three configured Savings vaults, not net worth and not net of Borrow debt |
| Base/Home animated header mark | Accepted existing component | Yes | Header navigation preserved | Existing focused component tests pass; final style pass did not alter its assets, animation, geometry, fonts, or hitboxes | Yes | Real-browser brand feedback remains user-controlled; no broad button selector applies to the mark |
| Funding and money actions | Integrated local implementation | Funding, USDC/ETH send/receive, Savings deposit/withdraw preparation, embedded trades, and one Borrow market | Existing authenticated boundaries and exact review flows | Full deterministic gate passed; Permit2 quote window is 180 seconds with 150-block freshness lag while permit max lifetime, nonce/balance/owner checks, original calldata and minimum received remain unchanged | Yes, when the required verified provider/session state exists | Live provider, wallet/signature, funded-wallet, receipt, and production authorization remain unperformed |

Invest list and discovery UI must stay disclosure-free: no restriction notes, contract `<details>`, source roster walls, or “not an endorsement” copy. Registry and docs may still record contracts and eligibility for builders. Account should gain a Disclosures / Terms section for that presentation; that settings destination is not built yet.

Money-action persistence: money actions require PostgreSQL in every runtime: set `DATABASE_URL` and `MONEY_ACTION_POSTGRES_CUTOVER=verified-empty` only after verifying no unresolved legacy SQLite money actions remain; otherwise they fail closed. CI runs the real PostgreSQL money/attempt contract against disposable PostgreSQL 14. This does not make all repository persistence PostgreSQL: the separate trading intent runtime uses local SQLite only when neither `VERCEL` nor `DATABASE_URL` is set, and fails closed for hosted or database-configured trading. See [Vercel deploy](vercel-deploy.md). This is not a production authorization or a claim of multi-instance swap readiness.

Known validation limits: no live CDP email/Base authentication, wallet signature, funds, live receipt/provider probe, deployment or production transfer browser harness was run. The seven production-component auth scenarios use mocked SDK/session boundaries and callback route observation, not live providers or full Next navigation. Local acceptance is not production authorization.
