# Invest

Page ID: [`333:13086`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13086).

`Invest trade proposal (#935, unreviewed)` [`430:6032`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=430-6032): Bitcoin detail entry, buy/sell amount, the simplified confirm (closed and Details open), recovery sheets, outcome toasts, desktop confirm, and a motion frame. Its design-only Icon `chevron-up` variant (`492:6781`) and the wrapping MoneyPrimaryAmount helper line need publishing only if selected.

`Invest asset detail proposal (#938, unreviewed)` [`422:4404`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=422-4404): Option A (full bleed, chosen by Jesse on 2026-09-26) at 390, 320 and 1440 px with the round-5 refinement (the standard `FinanceRow` for the balance, design-only `Stats` frames, a vendor footnote and a pinned trade bar), the B and C rows labelled not carried forward, a states row (drawn in the earlier B layout), notes `438:24624`, and the round-5 motion frame `438:20433` with GIF-over-still clips, timings and story paths. The section's design-only component sets are marked PROPOSED and stay inside it: `AssetChart` (`422:4648`, guide, axis and caption layers hidden), `AssetPriceHeader` (`424:4543`, vendor stamp hidden), `PositionSummary` (`424:4570`, unused by A), `FactRow` (`424:4571`, unused by A) and `TradeEntry` (`424:4604`, a placement stand-in because #935 owns Buy/Sell). `AssetDetailHeader` `166:1917` and `PriceChart` `166:1959` are unchanged. Its references group is `Reference / Chart-first asset detail (#938)` on References. The Storybook group is `Explorations/Invest asset detail`.
