# Figma workflow

Figma is Home's design source. Paper is retired.

- Figma file: [Home](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home)
- Library page: [Components](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=4-2) (`4:2`); current screens: [Home](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13084) (`333:13084`)
- Historical funded screen: [`Home — funded`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=13-19) (`13:19`), on the Archive page pending deletion (#841)
- Approved final design: [`Home — final`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=92-922) (`92:922`) — Jesse approved it on 2026-09-23 as the final Home design; production adoption is a separate issue
- Component and frame inventory: [figma-mapping.json](figma-mapping.json)
- Library sections (Foundations, Primitives, Finance patterns, Home components), gap matrix, Code Connect results and publish checklist: [design-system.md](design-system.md) (#791)
- File structure and the instances-only rule every pass keeps: [design-system.md](design-system.md#file-structure-and-the-instances-only-rule)
- Real-world references: [Mobbin](mobbin.md), through the [References](#references) workflow

## Page layout

Node IDs did not change when sections moved; existing node links still resolve. Page order and contents after #841:

| Page | Page ID | Contents |
| --- | --- | --- |
| Components | [`4:2`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=4-2) | Foundations `156:2094` → Primitives `158:1804` → Finance patterns `166:1704` → Home components `11:767`; finance pattern boards stay here. |
| Home | [`333:13084`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13084) | Screens `11:768` (`Home — final` `92:922`) → Home states `190:2821` → regional & desktop proposal #638 `311:12034` (kept whole, including its Add money/Cash out, Account and desktop frames). |
| Cash | [`333:13085`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13085) | Empty destination for future screens and proposals. |
| Invest | [`333:13086`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13086) | Empty destination for future screens and proposals. |
| Borrow | [`333:13087`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13087) | Empty destination for future screens and proposals. |
| Activity | [`333:13088`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13088) | Activity ledger proposal #637 `308:5918`. |
| Send / Add money / Cash out | [`333:13089`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13089) | Empty destination for future screens and proposals. |
| Account | [`333:13090`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13090) | Empty destination for future screens and proposals. |
| Card | [`333:13091`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13091) | Card proposal #636 `309:7257`. |
| Shell / desktop | [`333:13092`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13092) | Desktop proposal #694 `322:11819`. |
| References | [`333:13093`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13093) | Private Mobbin References `174:2891`; never share publicly. |
| Archive (pending deletion, #841) | [`333:13094`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13094) | Archive section `145:1638`: funded, v1, v2, explorations and annotations; deprecated MoneyNumpad `166:1739` and MoneyNumpadKey `166:1738`. |

For later runs, put a new screen on its area page: current screens first (left), each proposal in its own labelled section to the right, named `<Area/topic> proposal (#<issue>, unreviewed)`, with 200px between sections at y=0 and each section sized to contain its frames. New library components go into their section on Components; nothing but library goes on Components. Reference images go only on References. A script's page context resets to the first page (Components); switch to the target page with `setCurrentPageAsync`. #636, #637, #638 and #694 runs must place their next work on Card, Activity, Home and Shell / desktop respectively. PR #855 says section `308:5918` is "on the Home page"; its owner or Jesse should correct that to Activity. PR #853 links only its node ID and needs no page correction.

## Archive cleanup

Jesse's precondition for deletion was a named version, `Before archive cleanup (#841)`. It was **not saved**: the Figma Plugin API available to this run rejected `saveVersionHistoryAsync` as unsupported, and the Figma REST API has no endpoint to create a named version. The last automatic version before any #841 edit is `2402628470002735473`, at 2026-09-24 03:59:51 UTC. No nodes were deleted.

The Archive page `333:13094` holds section `145:1638` (funded `13:19`, v1 `65:503`, v2 `81:700`, explorations `54:140`, `54:322`, `54:577` and annotations), plus deprecated MoneyNumpad `166:1739` and MoneyNumpadKey `166:1738`. Card `12:28` (`inset=legacy`) still has 20 Archive instances and an existing Code Connect mapping. It is deleted with the Archive only once #684's `Card` template resolves to the set `269:5270`; otherwise it stays. To finish, Jesse saves the named version in Figma (File → Save to version history) or confirms the automatic version is enough; a follow-up then deletes the Archive page contents (`145:1638`, `166:1739`, `166:1738`), then `12:28` under that condition, and updates these docs.

## What belongs in Figma

A Figma component is something a designer places. Orchestration, providers, data wiring, connected wrappers, screen wrappers, and composition slots do not become Figma components.

Screens are frames composed only from instances ([the instances-only rule](design-system.md#file-structure-and-the-instances-only-rule)). `CardHeader`, `CardTitle`, `CardAction`, `ItemMedia`, `ItemContent`, `ItemTitle`, `ItemDescription`, and `ItemActions` remain named internal layers, matching their `data-slot` role in code; they are not separate designer-placeable components. `CardContent`, `DrawerContent`, `DrawerFooter`, and `FieldControl` are native Figma slots: content goes inside the instance's slot instead of a raw frame.

The historical `Home — funded` frame, now on the Archive page pending deletion, uses 12 designer-placeable families:

`ShellHeader`, `MoneyTicker`, `MoneyBreakdownItem`, `Button`, `Card`, `SectionHeader`, `MoneyGroupHeader`, `CurrencyMarkSlot`, `FinanceRow`, `MoreRow`, `TabItem`, and `TabBar`.

Since #789 the live route renders `Home — final`: the Save and Borrow product tiles, See all, and the grouped currency list are gone, and `HomeProductTile` and the `Button` product-tile variant were deleted. `Home — funded` keeps its tile compositions on the Archive page pending deletion.

The Home viewport remains 390×844. `ShellHeader` and `TabBar` are fixed around a vertically scrollable content frame whose DOM order mirrors `HomeOverview` (`apps/web/client/home/home-overview.tsx`): total balance, money actions, Your money, then the uncarded Activity feed. In `Home — final` those are `Card` instances (`TotalBalanceCard` as `variant=flush, inset=hero` and `YourMoneyCard` as `variant=default, inset=list`), and the feed is `ActivityHeading` (16px inset) over `ActivityRows` (4px inset, like `px-1`). On the Home page, Screens (`11:768`) holds `Home — final` (`92:922`), the production design since #789, and Home states (`190:2821`) beside it holds the no-Borrow, empty, partial, Activity error, loading, and status revealed states that the `Home/Overview` stories render; since #791 run 12 each frame uses the same `Card`, `ActivityHeading` and `ActivityRows` structure as `Home — final`.

The funded frame on the Archive page pending deletion intentionally preserves the credential-free browser fixture's mixed states below the fold: Save is loading and Activity shows its recovery treatment. The retained Figma inner-content export documents that below-fold structure; the retained browser comparison is the canonical 390×844 viewport, so below-fold browser parity remains a manual inspection rather than paired screenshot evidence. The Figma recovery treatment is simplified text plus a secondary action, while production uses a destructive `Alert`; that difference is retained explicitly rather than presented as exact fidelity.

## References

[Mobbin](mobbin.md) is the reference source for how shipped products solve the same problem; its plan, access path, smoke, and terms constraints live there.

- **When to consult.** Search Mobbin before any new screen, for any pattern question, and whenever a comment asks how others do it. Search `search_flows` for a journey and `search_screens` for one screen, with one plain-language intent per query and the platform that matches the Home surface (`ios` for the 390 px mobile layout, `web` for desktop). If Mobbin is unavailable, continue and say `Mobbin unavailable: <reason>` rather than inventing references.
- **How to cite.** Every reference is one line: app, screen or flow name, Mobbin URL, and the date it was consulted, for example `OKX — Apply for a loan (flow) — https://mobbin.com/flows/2938274e-a291-4ed1-81f8-0dd82685ce2c — 2026-09-23`. Cite the durable `mobbin_url`, never the expiring `image_url`. Citations go in the design PR (or the issue comment when there is no PR) and on the Figma References group label; images do not.
- **Where references go.** Selected screens live only in the [`References`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=174-2891) section (`174:2891`, created in #791) of the [Home Figma file](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home), on the private References page (`333:13093`), never on Components or an area page.
  - Inside References, group screens by pattern (for example `Borrow row`, `Loan application`, `Activity feed`) and label each group with its citations.
  - Whoever edits the Figma file places them: Jesse with Mobbin's Copy to Figma plugin, or a worker. The worker downloads the MCP `image_url` to a temporary directory outside the repository, converts WebP to JPEG (Figma cannot read WebP image metadata), fills placeholder rectangles with `upload_assets` `nodeIds` (`use_figma` cannot create images), and deletes the temporary files.
  - Keep a small curated set per pattern, and never commit, attach, or re-host the images anywhere else.
  - Reference screens are not Home components, are never instanced, and never get a Code Connect mapping.
- **Critique rule.** Every design PR has a `## References` section that names each reference by citation and says what the design borrowed from it and what it rejected, with the reason. A design with no relevant reference says so and names the queries it ran; a run without Mobbin says `Mobbin unavailable: <reason>`. The critic checks that the design adapts patterns to Home's tokens and components rather than copying another product's screen, artwork, or brand.

## Code Connect

Code Connect maps a Figma component to its code owner. It does not push code changes into Figma, rebuild a Figma component when React changes, or make Figma edits update production automatically. A worker still compares the current code and rendered route, updates Figma deliberately, and records the source SHA.

The 12 existing published families are mapped. Figma currently has a read-back defect: both mapping write paths report the components are already mapped, and `get_code_connect_suggestions` reports the selected component inventory is connected, while `get_code_connect_map` returns `{}`. Jesse approved that combined evidence for this file on 2026-09-22. Do not delete, disconnect, recreate, or replace mappings to work around the empty read-back.

Historical #683 status (2026-09-23): the comment passes changed component definitions without replacing stable family IDs. At that point Figma reported `CHANGED` for `MoneyBreakdownItem` (`12:20`), `Button` (`12:27`), `CurrencyMarkSlot` (`12:59`), `MoneyGroupHeader` (`12:39`), `TabItem` (`12:94`), `TabBar` (`12:95`, which nests `TabItem`), and `ShellHeader` (`11:773`, which gained `Show status` in #789), and `UNPUBLISHED` for the `FinanceRow` set (`96:1147`), `SignedBalanceBar` (`91:927`), and `ActivityLoader` (`91:932`). `FinanceRow` is one genericized row whose properties mirror the code API in [`apps/web/components/finance-rows.tsx`](../../apps/web/components/finance-rows.tsx) and its `ActivityRow` / `BalanceRow` / `AssetRow` wrappers: `Label`, `Context` (+`Show context`), `Value`, `Value context` (+`Show value context`), `Mark` (instance-swap of `CurrencyMarkSlot`), `Show chevron`, plus two variant dimensions — `Layout` (`Title aligned` default · `Centered (legacy)`) and `Value tone` (`Default` · `Success` · `Error` · `Muted`, value text bound to the `foreground` / `market-gain` / `destructive` / `muted-foreground` Home-token colors). The original component id `12:60` is preserved as the `Centered (legacy)` variant so the archived `Home — funded` keeps its prior mapping; it is never used in `Home — final`. At that earlier pass the unpublished nodes could not take a Code Connect mapping until Jesse published the Home Library; that earlier checklist is in [figma-mapping.json](figma-mapping.json) `componentInventory.publishChecklist`. Renaming Figma component properties or variant values does not change the node-to-file mapping. Do not delete, disconnect, recreate, or replace existing mappings to work around the empty read-back.

**Update (2026-09-23, #791).** The paragraph above is historical: the #683 revision has since been published. `FinanceRow` (`96:1147`) is mapped: `add_code_connect_map` reports it as already mapped. #789 mapped `SignedBalanceBar` (`91:927`) and `ActivityLoader` (`91:932`) to their components.

#791 added Foundations, Primitives, Finance patterns and References sections, and #841 moved References to its own page. Code Connect is owned by [#684](https://github.com/jessepollak/home/issues/684): its `.figma.tsx` templates, parse check and publish job supersede the 28 `add_code_connect_map` writes #841 made before that scope change. Keep node IDs stable when moving or deleting nodes so those templates keep resolving. The node-to-owner plan and publish checklist live in [design-system.md](design-system.md#code-connect), superseding `componentInventory.publishChecklist` in figma-mapping.json.

## Selected direction

[`Home — selected direction`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=65-503) combines the signed Borrow balance, three-row money summary, and continuous Activity feed requested across frames `54:140`, `54:322`, and `54:577`. It is a selected proposal pending Jesse approval, not the implementation baseline.

The Investments subtitle is data-driven content: `Across 1 asset` for singular and `Across N assets` for plural; the selected fixture renders `Across 2 assets`. The Figma fixture uses `USDC against Bitcoin · 5.1% APY` as directed. Production adoption must reconcile that design terminology with the existing variable-APR data and label contract rather than silently relabeling it.

The selected frame uses an intentional per-instance FinanceRow subtitle override so the borrowed asset, collateral, and rate remain readable at 390 px. It reduces that subtitle from the production `text-sm` treatment to 11.5 px with 14 px line height; the current Figma master and owned React row do not expose that typography as a reusable component capability. Production adoption must validate the exact selected copy, add an explicit owned variant only if approved, and reconcile the Figma `5.1% APY` terminology with the existing variable-APR data. Activity illustrates six retained rows and a centered continuation spinner; Figma does not prove pagination, cursor, retry, focus, account-switch, or reduced-motion behavior.

### Selected direction v2

[`Home — selected direction v2`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=81-700) (`81:700`) consolidates Jesse's three latest comments into one Figma-only iteration; v1 (`65:503`) and `Home — funded` (`13:19`) are on the Archive page pending deletion (#841). The Borrow summary row becomes `Borrow Cash` with the one-line subtitle `Borrow against your investments`, and the right value is `$30.01` with no leading minus and `5.1% APY` beneath it in muted text — the top allocation visualization keeps the neutral Borrow `−$30.01` treatment unchanged. Every row mark is now a real, license-clean imported SVG (see [figma-mapping.json](figma-mapping.json) `markAssets`): the US flag is a flat flag inset in a neutral gray circle, USDC/BTC/ETH use cryptocurrency-icons brand marks, and generic and borrow use lucide glyphs.

On the marks-library question, shadcn's default icon set is lucide (already used by this repo); no single shadcn registry covers flags plus fiat/crypto plus stocks, so the smallest honest combination is lucide (ISC) + flag-icons or circle-flags (MIT) + cryptocurrency-icons (CC0), with per-stock brand logos requiring a dedicated licensed source and the generic mark as the stock fallback for now. The v2 amount stays 14 px to match Cash and Investments and the subtitle is 12 px to fit one line; both remain per-instance overrides, not reusable master variants. This is design-source work only; no production dependency was added.

### Final design

[`Home — final`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=92-922) (`92:922`) is the approved final Home design (Jesse, 2026-09-23), synthesized from v2 with Jesse's comments resolved and fully componentized. Every Your money row and every Activity row is one genericized `FinanceRow` instance configured only through component properties; a programmatic audit (2026-09-23) reports 23 top-level instances, zero non-instance content layers, and zero nested overrides (instance roots carry only name, size, and layout positioning). `Home — funded` and v1/v2/explorations are now on the Archive page pending deletion (#841).

**Update (#791 run 11; #841 alignment noted below).** The two absolutely positioned `Card` background layers are gone. `TotalBalanceCard` and `YourMoneyCard` are `Card` instances whose slots hold the content, `MainViewport` is auto layout, and the feed's `HeadingGap` spacer became `ActivityHeading` / `ActivityRows`. Rows now render at the code height of 58px, with the amount on the title line and muted chevrons. The run-11 audit reports zero detached instances, raw stand-ins, unbound colours or generic names inside the frame.

- **Inline amounts (comment `1937331613`).** Cash, Investments, and Borrow Cash amounts sit inline on each row's title line in one shared right-aligned value column; chevrons share one column. Borrow Cash shows `$30.01` with `5.1% APR` right-aligned directly beneath (the `Value context` slot). This is the genericized `FinanceRow` (`Layout=Title aligned` + `Value context`), not a per-instance override. Figma locks instance-sublayer vertical layout (`stack-positioning`), so the layout is the component default rather than an instance edit. The final frame also applies the same row to Activity. #841 proposes vertically centring a lone value beside a two-line label; code still top-aligns it.
- **Heading belongs to its list (comment `1937331766`).** The `Your money` heading is now a 24 px `SectionHeader` with a 12 px gap to the first row and the card hugs its content, matching the Activity section's heading rhythm below. The pin sat in the empty area to the right of the heading, above the rows; tightening the vertical relationship makes the heading read as part of the list instead of a detached bar.
- **One genericized row (issue #683 follow-up).** All nine `Home — final` rows are instances of a single `FinanceRow` and differ only by component properties. `Value tone` (`Default`/`Success`/`Error`/`Muted`) exposes value colors as a property instead of nested fill overrides; the spurious container-frame fills were removed from the master so no instance carries a nested override. A `FinanceRow — usage reference` showcase (`116:1154`) in the Components section demonstrates the balance summary row, a balance with value context (Borrow Cash + APR), positive/negative Activity rows, an asset row, and the Success and Muted tones.
- **Money in is green; overdue is gray (comments `1938656305`, `1938656455`).** `Value tone=Success` is bound to a new `color/market-gain` Home token (#137333, the light `--market-gain` in `apps/web/app/globals.css`), not primary blue. On `Home — final`, money-in Activity values follow the `activity-presenter.ts` incoming direction: both Received rows and the Borrowed loan proceeds use `Success`; Sent (outgoing), Swapped (neutral), and Deposited to Savings (a self move between the user's accounts) stay `Default`. The usage reference's Overdue value uses `Muted`. Nothing on Home needs red money, so `Error` stays in the set and the all-variants board but is not featured in the usage reference. Production still maps `valueTone="success"` to `primary`, and Activity rows do not set `valueTone`; the adoption issue must change that.
- **Row insets match Home (comment `1938656119`).** The usage-reference cards are fixed on the container, not per instance: 358 px wide with 16 px left/right padding and the stroke excluded from layout, so every row starts 32 px from the board edge and is 326 px wide, the same as `Home — final`.
- **Publishable library (issue #683 run 24).** The Components section (`11:767`) is grouped under seven category headings — Shell, Money / balance, Actions, Rows, Marks, Feedback / loading, Navigation — with each component or set placed once and described with its code owner or as design-only. Variant values use sentence case (`Kind=Cash`, `Variant=Primary`, `Icon=Home`). The loose `Button / no icon` sample instance and its label were removed, since `Show icon` already covers it; no component or set was deleted. The `FinanceRow — all variants` board (`131:1285`) covers the `Layout × Value tone` matrix, property toggles, marks, and stress cases. Pairwise audits report zero overlaps in Components, Screens, and History. In a Figma component set the text-property default is shared by every variant, so variant definitions of `MoneyBreakdownItem`, `Button`, and `TabItem` show one sample label; instances override it.

#789 adopted `Home — final` in production: the title-aligned `FinanceRow` layout with the `Value context` slot, `valueTone="success"` bound to `--market-gain` for money in, `SignedBalanceBar` (`apps/web/components/signed-balance-bar.tsx`), and `ActivityLoader` (`apps/web/components/activity-loader.tsx`). Jesse chose `N% APR` for Borrow Cash (loans quote APR; Cash stays APY), and the Figma copy now reads `5.1% APR`. The remaining residual differences live in [figma-mapping.json](figma-mapping.json) `residualDifferences`.

The `Home states` section (`190:2821`) follows Jesse's two state reviews, and nothing on Home is red for a load error. A balance read failure shows a small status icon beside the profile picture (the `ShellHeader` `Show status` property; `HomeHeaderStatus` in code). Tapping it opens a popover with the message and a Reload icon button, shown in the `status revealed` frame (`246:4067`); with no country set, the popover offers Open Account instead. An Activity read failure stays in Activity as a centered, muted `Activity unavailable` line with a reload icon. Unread rows show a muted `—` with no chevron. Loading rows use a glyphless shimmer mark in the skeleton tone with no chevron. The Cash and Investments rows use lucide `banknote` and `chart-line` glyphs instead of a flag or token mark. An empty Activity feed offers a centered 44 px `Add money` prompt. Production flags are Twemoji (CC-BY 4.0), not the flag-icons or circle-flags candidates named above.

## Comment pass

1. Jesse comments or edits the relevant Figma page.
2. Jesse comments `process Figma comments` on issue #683.
3. The worker reads comments through `GET /v1/files/ixgttt6IurKynsvMJpLYDC/comments` using a private token file outside the repo. The token is loaded only into the request environment and is never printed, captured, or committed.
4. The worker reads changed frames and component structure through the Figma MCP, compares them with the current React owner and a real browser render, and classifies each thread.
5. The worker revises the existing owned nodes in place with `use_figma`, or implements an approved result through the production-component Storybook/code loop. Existing published component IDs and Code Connect mappings are preserved unless Jesse explicitly authorizes a migration.
6. The worker replies to every newly processed thread through the Figma comments REST API, reports component-definition changes that need publication, refreshes screenshots, and records residual differences. Older answered threads are not duplicated. The REST comments API cannot set `resolved_at` (no endpoint; `POST`/`PUT`/`resolve` variants return 404) and the Plugin API does not expose comments, so verified-satisfied threads are reported for a single manual Figma-UI resolve action rather than closed programmatically. The exact open-and-satisfied thread IDs live in [figma-mapping.json](figma-mapping.json) `commentHygiene`.

Figma comments are product direction, not automatic code authorization. A Jesse-directed synthesis remains unapproved until Jesse approves that rendered frame. Code adoption then follows the production-component Storybook, browser-validation, and current-head evidence contracts.
