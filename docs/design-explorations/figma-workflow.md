# Figma workflow

Figma is Home's design source. Paper is retired.

- Figma file: [Home](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home)
- Library page: [Components](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=4-2) (`4:2`); current screens: [Home](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13084) (`333:13084`)
- Approved final design: [`Home — final`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=92-922) (`92:922`) — Jesse approved it on 2026-09-23 as the final Home design; production adoption is a separate issue
- Component and frame inventory: [figma-mapping.json](figma-mapping.json)
- Library sections (Foundations, Primitives, Finance patterns, Home components), gap matrix, Code Connect results and publish checklist: [design-system.md](design-system.md) (#791)
- File structure and the instances-only rule every pass keeps: [design-system.md](design-system.md#file-structure-and-the-instances-only-rule)
- Real-world references: [Mobbin](mobbin.md), through the [References](#references) workflow
- Card proposal (unreviewed): [`Card proposal (#636, unreviewed)`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=309-7257) (`309:7257`) on the Card page holds 23 frames in three labelled rows, revision 3 after Jesse's 2026-09-24 review: landing states (Lock replaces Freeze; the outage uses the header status) and desktop; entry, staged verification, wallet variants and Card settings with Home's Home · Card · Invest navigation; and the Activity, Card details, Get help and Replace/Cancel sheets on one spacing spec. A Notes line lists the deferred settings and where in-app support (#813) will enter. Every frame is built from Components-page library instances. Its additive library pieces are the Icon `credit-card`, `snowflake`, `eye`, `lock`, `smartphone-nfc` and `settings` variants, CurrencyMarkSlot `Card`, `Freeze`, `Lock`, `Settings`, `Wallet` and `Cancel`, TabItem `Card`, the Alert `Action` text property and hugging MoneyTicker text. The design-only sets `TabBar / with Card` (`309:7317`), `CardArt` (`309:7326`, now `status` × `size` with `status=locked` and a large `size=lg`) and `CardControlRow` (`309:7499`) live in Components → Home components → Card, marked DESIGN-ONLY until Jesse selects a direction. The capability map is [card-design.md](../card-design.md). Nothing was published.
- Unreviewed [Activity ledger proposal](../activity-ledger-design.md): section [`308:5918`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=308-5918) on the Activity page (`333:13088`), not adopted.
- Unreviewed [Borrow overview proposal](../borrow-overview-design.md): section [`438:22134`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=438-22134) on the Borrow page (`333:13087`), with its motion frame `441:5563`; design-only `CurrencyMarkSlot` `Kind=Dogecoin`, `Kind=XRP` and `Kind=Cardano` variants were added to Components and need publishing only if selected. Not adopted.

## Page layout

Surviving node IDs did not change when sections moved. Current page order and contents after #882:

See the [Figma page directory](figma-pages/) for each page's ID and current contents. Add a page file rather than another row in this guide.

For later runs, put a new screen on its area page: current screens first (left), each proposal in its own labelled section to the right, named `<Area/topic> proposal (#<issue>, unreviewed)`, with 200px between sections at y=0 and each section sized to contain its frames. New library components go into their section on Components; nothing but library goes on Components. Reference images go only on References. A script's page context resets to the first page (Components); switch to the target page with `setCurrentPageAsync`. #636, #637, #638 and #694 runs must place their next work on Card, Activity, Home and Shell / desktop respectively. PR #855 places section `308:5918` on Activity. PR #853 links only its node ID and needs no page correction.

## Archive cleanup

For #882, Jesse deleted the Archive page `333:13094`, including section `145:1638` (funded `13:19`, v1 `65:503`, v2 `81:700`, explorations `54:140`, `54:322`, `54:577` and annotations). #882 then deleted MoneyNumpad `166:1739`, MoneyNumpadKey `166:1738` and Card's legacy variant `12:28`. Those components had zero live instances before deletion; the whole-file audit afterwards found 1,435 instances and zero missing-component instances. Restore point: [Full mapping](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?version-id=2402808592851949919) (2026-09-24 14:44 UTC). Jesse must publish the library for these deletions to leave the published library.

## Design runs that edit Figma

### Edit-access preflight

Once, at the start of a run whose issue asks for Figma work and before its first Storybook or Figma change, test the Figma edit connection the run will use: list the pages, read the target area page from [Page layout](#page-layout) (and the issue's section when one already exists), remove any leftover probe for this issue (a node named `edit-access probe (#<issue>)`, anywhere in the file), then create a hidden rectangle with that name directly on the target page, record its node ID, and remove it. The target page always exists, so a run whose proposal section does not exist yet passes the preflight first and creates the section afterwards.

A run whose proposal includes motion also proves, in the same preflight, every capability the GIF step in [Motion frames](#motion-frames) needs, so it cannot discover a missing one after changing Storybook. First check that `ffmpeg -version` succeeds (see the README prerequisites). Then make a disposable GIF with it (for example `ffmpeg -f lavfi -i color=c=gray:s=8x8:d=0.2 -loop 0 probe.gif`), fill the probe rectangle with that GIF through the Figma asset upload, targeting the probe's node ID, before removing the probe, and read the probe back to confirm it now has an image fill. Removing the probe removes the fill with it.

If reading, writing, or removing a probe fails, or a motion run has no `ffmpeg` or its probe upload fails or leaves no image fill, stop and make no Storybook-only change. The public handoff comment names the failing step and a redacted error summary: the status code and short error class (for example `write: 403 Forbidden`, `upload: 415 Unsupported content type`, or `ffmpeg: not found`), with no hostnames, local paths, URLs, tokens, request IDs or raw response bodies. Raw error output stays in the run's private log and never enters an issue, PR or commit. Never carry “Figma couldn't connect” as a residual difference while continuing the run.

Whenever a run stops with a probe still in the file (its own, or a leftover it could not remove), the handoff also names each probe's page and node ID (and its section, if it sits in one). The issue's next design run owns the cleanup through its preflight. If the issue closes first, whoever closes it deletes the named node by hand, and only that node.

**Why (#853).** Later #853 runs used a direct Figma connection whose sign-in was refused with `403 Forbidden`; each recorded the failure as a residual difference and kept changing Storybook, leaving the Figma frames stale. Earlier #853 runs and #896 edited Figma through an authenticated connection. No up-front check was required. #908 run 2 stopped at this preflight, reported the reason, and made no change.

### Motion frames

Every motion frame in a design proposal carries these items each round:

1. **Recording and stills.** Record each motion story from Storybook at the proposal viewport (390×844 CSS px for mobile) with the pinned `agent-browser`: open the story and let it settle, `record start <file>.mp4`, replay the entrance with Replay or a reload, hold about 1 s after it settles, then `record stop`. Convert the recording to a looping GIF cropped to the moving element, starting about 0.5 s before the replay so its first frame is the settled state rather than a blank canvas, and extract 2–3 still keyframes from the same recording. For example:

   ```bash
   ffmpeg -ss <replay - 0.5> -t 2.1 -i clip.mp4 -vf "fps=30,crop=<w>:<h>:<x>:<y>,split[a][b];[a]palettegen=stats_mode=diff[p];[b][p]paletteuse=dither=none" -loop 0 clip.gif
   ```

2. **Placement.** Put the GIF beside the still keyframes in the frame. The Figma edit connection cannot create images: create placeholder rectangles through it, then fill them through the Figma asset upload, targeting those rectangles' node IDs. Then give the clip rectangle two image fills, the settled still underneath and the GIF on top. Figma's export renderer (screenshots and `exportAsync`) draws a GIF fill blank; with the still underneath, the #908 clip cells export as the settled frame instead. Place the GIF automatically every round, with no manual step.
3. **Labels.** Show the duration (longest delay + duration among the story's animations, for example `560 ms, then still`) and easing (the named Home curve and its `cubic-bezier`).
4. **Links.** Hyperlink the story and its reduced-motion story on the commit-specific Storybook deployment of the recorded head, so later pushes cannot change the target. If the subject has no reduced-motion story, link the nearest story that shares its reduced-motion rule and label the gap in the frame.
5. **PR clip.** Keep the MP4 in the PR per [UI PR previews](../ui-pr-previews.md).

The [`B — motion clips (#908)` frame](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=406-15867) (`406:15867`) is an example. It sits in its own section (`406:16467`) at the right end of Components, beside the #896 illustration proposal (`379:3860`), and shows the #905 entrances as recorded at `8aeb966`: four entrances, each with three stills, a GIF, duration, easing and both links. Savings, money moving and transfer complete link the card's reduced-motion story because #905 has none for those subjects.

### Clip formats

| Format | Can a design run place it? | Canvas | Present / prototype |
| --- | --- | --- | --- |
| GIF | Yes. The Figma asset upload accepts PNG, JPEG, GIF, WebP and SVG and stores a GIF as an image fill. | Still frame, per Figma's docs. | Plays, per Figma's docs. |
| Video (MP4, MOV, WebM) | No. The Figma edit connection rejects `createVideoAsync` as “not a supported API”; the asset upload returns `415 Unsupported content type: video/mp4`; Figma's REST API documents no upload endpoint. A person with a paid seat can drag a video into a frame by hand. | Still frame, per Figma's docs. | Plays, per Figma's docs. |

Canvas and present-mode playback in the Home file is unverified: the rows above restate Figma's documentation until Jesse confirms what plays. Reviewers open the motion frame in present mode to watch a clip, and use the PR's MP4 for a sharp copy. Jesse chose GIF-in-frame placed automatically each round with the MP4 kept in the PR (#908).

## Source of truth

Code is the truth for shipped UI; Figma is the truth for proposals. A proposal is accepted only when a Storybook story exists for it. Code Connect maps Figma → code; the variables sync pushes tokens code → Figma; nothing edits code from Figma automatically.

- **Code Connect (Figma → code).** [`apps/web/figma-components.json`](../../apps/web/figma-components.json) is the single source of truth for Code Connect node ownership: it maps each Figma node to its code owner, and [figma-mapping.json](figma-mapping.json) and [design-system.md](design-system.md#code-connect) point to it instead of keeping their own lists. Each mapped component has a Code Connect template file (`*.figma.ts`, the maintained template format; the React and Storybook parsers are deprecated) under the design-lane `explorations/code-connect/` directory of `apps/web/client/` or `apps/web/components/`, mirroring its source path (see [design-lane code](README.md)). `apps/web/figma.config.json` limits Code Connect to those two directories, and the dead-code gate treats them like any other exploration. The mapping also lists the Figma components that are not mapped and the code components that have no Figma component yet.
- **Figma frame beside each story.** `@storybook/addon-designs` shows the mapped Figma node in a Design panel for every story listed in the mapping (`parameters.design`).
- **Variables (code → Figma).** `apps/web/scripts/figma-variables.mjs` reads the custom properties in `apps/web/app/globals.css` and upserts them into the existing `Home tokens` collection (new `space/*` go to `Home scale`) through `POST /v1/files/:key/variables`. It reuses variables by `var(--…)` code syntax, then by name, so the collection #683 built by hand is the same collection, not a duplicate. Light values come from `:root`, Dark from `.dark`. It never deletes, never overwrites a variable whose description contains the whole word `PROPOSED` or a mode value that is an alias (both reported as skipped), leaves Figma-only tokens such as `warning` alone, reports unmatched Figma variables without changing them, fills in missing WEB code syntax for name-matched variables, and skips font families because Figma binds SF Pro stand-ins. `globals.css` defines no font sizes, weights or line heights, so none are synced.
- **Commands** (from `apps/web`): `bun run figma:connect:parse` (offline template check, also a CI step), `bun run figma:connect:publish`, `bun run figma:variables:dry-run` (prints the plan and writes nothing; with `FIGMA_ACCESS_TOKEN` it diffs against the live file, without one it plans against an empty file; `--json` prints the machine plan), `bun run figma:variables`.
- **CI.** On pushes to `main`, the `publish Code Connect` and `sync Figma variables` jobs run only when the `FIGMA_ACCESS_TOKEN` repository secret is set; without it they skip and pass. The token needs the scopes `code_connect:write` (publish) and `file_variables:read` + `file_variables:write` (variables; the Variables REST API needs a Figma Enterprise seat).
- **UI-created mappings.** Publishing does not overwrite a node that already has a mapping created in Figma's Code Connect UI; the CLI skips it with a warning. Replacing those needs `figma connect publish --force` and Jesse's authorization (see [Code Connect](#code-connect)).

## Regional and desktop proposal (#638)

The [regional and desktop proposal](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=311-12034) section `Regional & desktop proposal (#638, unreviewed)` (`311:12034`) sits on the [Home page](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13084) (`333:13084`), 200px right of Home states, without overlap. It is **unreviewed** and not a production-route change. Instances resolve to Components library components, with three exceptions kept inside the section: the five proposal-local `PROPOSED Mark` icons, the Display currency Select detached to hold four 44px options (the master has three 32px options), and the #694 `PrimaryNavigation` rail detached to add Card. The phone Home · Card · Invest bar is the library's design-only `TabBar / with Card` (#636). Rows follow the row-alignment rule (single-element sides centred), and the Activity loading mark is centred in its 56px row as `ActivityLoader` renders in code; the library master still top-aligns it. Jesse's Figma review (2026-09-24) asked for seven changes, and both Storybook and the section now carry them: local currency inside Cash for BR and ID (NG remains US dollars), no country-not-set state (frame `311:13336` is deleted), Deposit copy, the local currency name on Cash out unavailable, the #694 rail with Card from #636 and one 640px content column (a deliberate difference from #694's Home/Invest rail and its latest Activity-right grid), a centered 480px modal with 16px corners on every side and no swipe handle, and the Display currency dropdown with a `Country default (<currency>)` option followed by explicit currencies excluding that country's default. The desktop cash-out review frame `313:12047` follows production's review order: Provider, Payout app, Payout handle, From as the executing address, Asset, Network, Provider fee, `Approximate receive` with the ≈ marker, and Estimated delivery above the approximate-amount note. Every Account frame (`312:11058`, `312:11314`, `312:11500`, `312:11751`) shows Display currency as the Select. The section's residual-differences note records what the library cannot yet match: the SignedBalanceBar and its legend use fixed widths, so bars do not stretch at desktop or 320px and long local amounts wrap in the legend, and Activity rows are illustrative fixtures that differ from the story fixtures. Review the rendered production-component proposals under Storybook IDs `explorations-regional-home--us` (and sibling states), `explorations-regional-money-sheets--add-money-us` (and sibling sheets), and `explorations-regional-preferences--first-use-aligned-defaults` (and sibling preferences); the full story inventory is in [design-system.md](../design-system.md#component-workshop). Amounts and availability shown are illustrative fixtures. Jesse selects before any adoption.

## What belongs in Figma

A Figma component is something a designer places. Orchestration, providers, data wiring, connected wrappers, screen wrappers, and composition slots do not become Figma components.

Screens are frames composed only from instances ([the instances-only rule](design-system.md#file-structure-and-the-instances-only-rule)). `CardHeader`, `CardTitle`, `CardAction`, `ItemMedia`, `ItemContent`, `ItemTitle`, `ItemDescription`, and `ItemActions` remain named internal layers, matching their `data-slot` role in code; they are not separate designer-placeable components. `CardContent`, `DrawerContent`, `DrawerFooter`, and `FieldControl` are native Figma slots: content goes inside the instance's slot instead of a raw frame.

The deleted `Home — funded` frame used 12 designer-placeable families:

`ShellHeader`, `MoneyTicker`, `MoneyBreakdownItem`, `Button`, `Card`, `SectionHeader`, `MoneyGroupHeader`, `CurrencyMarkSlot`, `FinanceRow`, `MoreRow`, `TabItem`, and `TabBar`.

Since #789 the live route renders `Home — final`: the Save and Borrow product tiles, See all, and the grouped currency list are gone, and `HomeProductTile` and the `Button` product-tile variant were deleted. Its earlier tile compositions were deleted with the historical frame in #882.

The Home viewport remains 390×844. `ShellHeader` and `TabBar` are fixed around a vertically scrollable content frame whose DOM order mirrors `HomeOverview` (`apps/web/client/home/home-overview.tsx`): total balance, money actions, Your money, then the uncarded Activity feed. In `Home — final` those are `Card` instances (`TotalBalanceCard` as `variant=flush, inset=hero` and `YourMoneyCard` as `variant=default, inset=list`), and the feed is `ActivityHeading` (16px inset) over `ActivityRows` (4px inset, like `px-1`). On the Home page, Screens (`11:768`) holds `Home — final` (`92:922`), the production design since #789, and Home states (`190:2821`) beside it holds the no-Borrow, empty, partial, Activity error, loading, and status revealed states that the `Home/Overview` stories render; since #791 run 12 each frame uses the same `Card`, `ActivityHeading` and `ActivityRows` structure as `Home — final`.

The deleted funded frame had shown the credential-free browser fixture's mixed states below the fold: Save loading and Activity recovery. Its earlier Figma inner-content export documented that structure; the browser comparison captured only the canonical 390×844 viewport, not paired below-fold screenshot evidence. The former Figma recovery treatment used simplified text and a secondary action, while production uses a destructive `Alert`.

## References

[Mobbin](mobbin.md) is the reference source for how shipped products solve the same problem; its plan, access path, smoke, and terms constraints live there.

- **When to consult.** Search Mobbin before any new screen, for any pattern question, and whenever a comment asks how others do it. Search `search_flows` for a journey and `search_screens` for one screen, with one plain-language intent per query and the platform that matches the Home surface (`ios` for the 390 px mobile layout, `web` for desktop). If Mobbin is unavailable, continue and say `Mobbin unavailable: <reason>` rather than inventing references.
- **How to cite.** Every reference is one line: app, screen or flow name, Mobbin URL, and the date it was consulted, for example `OKX — Apply for a loan (flow) — https://mobbin.com/flows/2938274e-a291-4ed1-81f8-0dd82685ce2c — 2026-09-23`. Cite the durable `mobbin_url`, never the expiring `image_url`. Citations go in the design PR (or the issue comment when there is no PR) and on the Figma References group label; images do not.
- **Where references go.** Selected screens live only in the [`References`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=174-2891) section (`174:2891`, created in #791) of the [Home Figma file](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home), on the private References page (`333:13093`), never on Components or an area page.
  - Inside References, group screens by pattern (for example `Borrow row`, `Loan application`, `Activity feed`) and label each group with its citations.
  - Whoever edits the Figma file places them: Jesse with Mobbin's Copy to Figma plugin, or a worker. The worker downloads the MCP `image_url` to a temporary directory outside the repository, converts WebP to JPEG (Figma cannot read WebP image metadata), fills placeholder rectangles through the Figma asset upload targeting their node IDs (the Figma edit connection cannot create images), and deletes the temporary files.
  - Keep a small curated set per pattern, and never commit, attach, or re-host the images anywhere else.
  - Reference screens are not Home components, are never instanced, and never get a Code Connect mapping.
- **Critique rule.** Every design PR has a `## References` section that names each reference by citation and says what the design borrowed from it and what it rejected, with the reason. A design with no relevant reference says so and names the queries it ran; a run without Mobbin says `Mobbin unavailable: <reason>`. The critic checks that the design adapts patterns to Home's tokens and components rather than copying another product's screen, artwork, or brand.

## Code Connect

Code Connect maps a Figma component to its code owner. It does not push code changes into Figma, rebuild a Figma component when React changes, or make Figma edits update production automatically. A worker still compares the current code and rendered route, updates Figma deliberately, and records the source SHA.

The 12 existing published families are mapped. Figma currently has a read-back defect: both mapping write paths report the components are already mapped, and `get_code_connect_suggestions` reports the selected component inventory is connected, while `get_code_connect_map` returns `{}`. Jesse approved that combined evidence for this file on 2026-09-22. Do not delete, disconnect, recreate, or replace mappings to work around the empty read-back.

Historical #683 status (2026-09-23): the comment passes changed component definitions without replacing stable family IDs. At that point Figma reported `CHANGED` for `MoneyBreakdownItem` (`12:20`), `Button` (`12:27`), `CurrencyMarkSlot` (`12:59`), `MoneyGroupHeader` (`12:39`), `TabItem` (`12:94`), `TabBar` (`12:95`, which nests `TabItem`), and `ShellHeader` (`11:773`, which gained `Show status` in #789), and `UNPUBLISHED` for the `FinanceRow` set (`96:1147`), `SignedBalanceBar` (`91:927`), and `ActivityLoader` (`91:932`). `FinanceRow` is one genericized row whose properties mirror the code API in [`apps/web/components/finance-rows.tsx`](../../apps/web/components/finance-rows.tsx) and its `ActivityRow` / `BalanceRow` / `AssetRow` wrappers: `Label`, `Context` (+`Show context`), `Value`, `Value context` (+`Show value context`), `Mark` (instance-swap of `CurrencyMarkSlot`), `Show chevron`, plus two variant dimensions — `Layout` (`Title aligned` default · `Centered (legacy)`) and `Value tone` (`Default` · `Success` · `Error` · `Muted`, value text bound to the `foreground` / `market-gain` / `destructive` / `muted-foreground` Home-token colors). The original component id `12:60` remains the `Centered (legacy)` variant; its sole instance is on the Components review board, not `Home — final`. At that earlier pass the unpublished nodes could not take a Code Connect mapping until Jesse published the Home Library; that earlier checklist is in [figma-mapping.json](figma-mapping.json) `componentInventory.publishChecklist`. Renaming Figma component properties or variant values does not change the node-to-file mapping. Do not delete, disconnect, recreate, or replace existing mappings to work around the empty read-back.

**Update (2026-09-23, #791).** The paragraph above is historical: the #683 revision has since been published. `FinanceRow` (`96:1147`) is mapped: `add_code_connect_map` reports it as already mapped. #789 mapped `SignedBalanceBar` (`91:927`) and `ActivityLoader` (`91:932`) to their components.

#791 added Foundations, Primitives, Finance patterns and References sections, and #841 moved References to its own page. Code Connect is owned by [#684](https://github.com/jessepollak/home/issues/684): its `*.figma.ts` templates, parse check and publish job supersede the 28 `add_code_connect_map` writes #841 made before that scope change. Keep surviving mapped node IDs stable so their templates keep resolving. Node ownership lives in [`apps/web/figma-components.json`](../../apps/web/figma-components.json); the publish checklist lives in [design-system.md](design-system.md#publish-checklist), superseding `componentInventory.publishChecklist` in figma-mapping.json.

**Update (2026-09-24, #684).** Every node in `apps/web/figma-components.json` `components` (39 after #841) has a Code Connect template file in the repository. Each ID was re-read from the live file after #841's page moves and still resolves to the same component or set; `Card` targets the reconciled set `269:5270`. #841 deprecated `MoneyNumpad` (`166:1739`), and #882 deleted it and its Figma sub-part (`166:1738`); neither has a template. Their earlier published definitions remain until Jesse publishes the library deletions. On publish, the CLI skips any node that already has a mapping created in Figma's Code Connect UI or through `add_code_connect_map` (label `React`). The last publish (04:3x UTC, before #841) uploaded only `MoneyNumpad`; the other nodes have UI-created mappings. An earlier publish that day uploaded 29 templates before the #791 run added its hand mappings. Replacing the UI mappings with the templates (which also clears the stale `home-panel.tsx` owners above) needs `bun run figma:connect:publish -- --force`, run once with Jesse's authorization. After that, the CI job keeps the templates current without `--force`. Until then, stop adding hand mappings for nodes listed in `apps/web/figma-components.json`. `get_code_connect_map` still reads back `{}` for this file.

## Historical selected direction

Before #882 deleted the superseded frames, `Home — selected direction` (`65:503`) combined the signed Borrow balance, three-row money summary, and continuous Activity feed requested across explorations `54:140`, `54:322`, and `54:577`. It was a selected proposal, not the implementation baseline. Its fixture rendered the data-driven Investments subtitle `Across 2 assets` (singular `Across 1 asset`) and used `USDC against Bitcoin · 5.1% APY` for Borrow; the final design instead uses APR.

The deleted frame used a per-instance FinanceRow subtitle override to fit borrowed asset, collateral, and rate at 390 px: 11.5 px type with 14 px line height rather than production's `text-sm`. This was not a reusable master variant. Its Activity illustration showed six rows and a continuation spinner, not evidence of pagination, retry, focus, account-switch, or reduced-motion behavior.

### Historical selected direction v2

The deleted `Home — selected direction v2` (`81:700`) had consolidated Jesse's three comments into a Figma-only iteration. Its Borrow summary row became `Borrow Cash` with `Borrow against your investments` underneath, `$30.01` and muted `5.1% APY` at right; the allocation retained the neutral Borrow `−$30.01`. Its row marks used imported SVGs (see [figma-mapping.json](figma-mapping.json) `markAssets`): a flat US flag in a neutral circle, cryptocurrency-icons USDC/BTC/ETH marks and lucide generic/borrow glyphs. The v2 amount was 14 px and the subtitle 12 px, both per-instance overrides rather than reusable master variants. It added no production dependency.

### Final design

[`Home — final`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=92-922) (`92:922`) is the approved final Home design (Jesse, 2026-09-23), synthesized from v2 with Jesse's comments resolved and fully componentized. Every Your money row and every Activity row is one genericized `FinanceRow` instance configured only through component properties; a programmatic audit (2026-09-23) reports 23 top-level instances, zero non-instance content layers, and zero nested overrides (instance roots carry only name, size, and layout positioning). `Home — funded` and v1/v2/explorations were deleted in #882.

**Update (#791 run 11; #841 alignment noted below).** The two absolutely positioned `Card` background layers are gone. `TotalBalanceCard` and `YourMoneyCard` are `Card` instances whose slots hold the content, `MainViewport` is auto layout, and the feed's `HeadingGap` spacer became `ActivityHeading` / `ActivityRows`. Rows now render at the code height of 58px, with the amount on the title line and muted chevrons. The run-11 audit reports zero detached instances, raw stand-ins, unbound colours or generic names inside the frame.

- **Inline amounts (comment `1937331613`).** Cash, Investments, and Borrow Cash amounts sit inline on each row's title line in one shared right-aligned value column; chevrons share one column. Borrow Cash shows `$30.01` with `5.1% APR` right-aligned directly beneath (the `Value context` slot). This is the genericized `FinanceRow` (`Layout=Title aligned` + `Value context`), not a per-instance override. Figma locks instance-sublayer vertical layout (`stack-positioning`), so the layout is the component default rather than an instance edit. The final frame also applies the same row to Activity. #841 proposes vertically centring a lone value beside a two-line label; code still top-aligns it.
- **Heading belongs to its list (comment `1937331766`).** The `Your money` heading is now a 24 px `SectionHeader` with a 12 px gap to the first row and the card hugs its content, matching the Activity section's heading rhythm below. The pin sat in the empty area to the right of the heading, above the rows; tightening the vertical relationship makes the heading read as part of the list instead of a detached bar.
- **One genericized row (issue #683 follow-up).** All nine `Home — final` rows are instances of a single `FinanceRow` and differ only by component properties. `Value tone` (`Default`/`Success`/`Error`/`Muted`) exposes value colors as a property instead of nested fill overrides; the spurious container-frame fills were removed from the master so no instance carries a nested override. A `FinanceRow — usage reference` showcase (`116:1154`) in the Components section demonstrates the balance summary row, a balance with value context (Borrow Cash + APR), positive/negative Activity rows, an asset row, and the Success and Muted tones.
- **Money in is green; overdue is gray (comments `1938656305`, `1938656455`).** `Value tone=Success` is bound to a new `color/market-gain` Home token (#137333, the light `--market-gain` in `apps/web/app/globals.css`), not primary blue. On `Home — final`, money-in Activity values follow the `activity-presenter.ts` incoming direction: both Received rows and the Borrowed loan proceeds use `Success`; Sent (outgoing), Swapped (neutral), and Deposited to Savings (a self move between the user's accounts) stay `Default`. The usage reference's Overdue value uses `Muted`. Nothing on Home needs red money, so `Error` stays in the set and the all-variants board but is not featured in the usage reference. Production still maps `valueTone="success"` to `primary`, and Activity rows do not set `valueTone`; the adoption issue must change that.
- **Row insets match Home (comment `1938656119`).** The usage-reference cards are fixed on the container, not per instance: 358 px wide with 16 px left/right padding and the stroke excluded from layout, so every row starts 32 px from the board edge and is 326 px wide, the same as `Home — final`.
- **Publishable library (issue #683 run 24).** The Components section (`11:767`) is grouped under seven category headings — Shell, Money / balance, Actions, Rows, Marks, Feedback / loading, Navigation — with each component or set placed once and described with its code owner or as design-only. Variant values use sentence case (`Kind=Cash`, `Variant=Primary`, `Icon=Home`). The loose `Button / no icon` sample instance and its label were removed, since `Show icon` already covers it; no component or set was deleted. The `FinanceRow — all variants` board (`131:1285`) covers the `Layout × Value tone` matrix, property toggles, marks, and stress cases. Pairwise audits report zero overlaps in Components, Screens, and History. In a Figma component set the text-property default is shared by every variant, so variant definitions of `MoneyBreakdownItem`, `Button`, and `TabItem` show one sample label; instances override it.

#789 adopted `Home — final` in production: the title-aligned `FinanceRow` layout with the `Value context` slot, `valueTone="success"` bound to `--market-gain` for money in, `SignedBalanceBar` (`apps/web/components/signed-balance-bar.tsx`), and `ActivityLoader` (`apps/web/components/activity-loader.tsx`). Jesse chose `N% APR` for Borrow Cash (loans quote APR; Cash stays APY), and the Figma copy now reads `5.1% APR`. The remaining residual differences live in [figma-mapping.json](figma-mapping.json) `residualDifferences`.

The `Home states` section (`190:2821`) follows Jesse's two state reviews, and nothing on Home is red for a load error. A balance read failure shows a small status icon beside the profile picture (the `ShellHeader` `Show status` property; `HomeHeaderStatus` in code). Tapping it opens a popover with the message and a Retry icon button, shown in the `status revealed` frame (`246:4067`); with no country set, the popover offers Open Account instead. An Activity read failure stays in Activity as a centered, muted `Activity unavailable` line with a reload icon. Unread rows show a muted `—` with no chevron. Loading rows use a glyphless shimmer mark in the skeleton tone with no chevron. The Cash and Investments rows use lucide `banknote` and `chart-line` glyphs instead of a flag or token mark. An empty Activity feed offers a centered 44 px `Add money` prompt. Production flags are Twemoji (CC-BY 4.0), not the flag-icons or circle-flags candidates named above.

## Desktop proposal (#694, unreviewed)

[`Desktop proposal (#694, unreviewed)`](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=322-11819) (`322:11819`) proposes a desktop layout with a Home / Invest side nav, from main `476735ca`. It is the only section on the [Shell / desktop](https://www.figma.com/design/ixgttt6IurKynsvMJpLYDC/Home?node-id=333-13092) page (`333:13092`), at x=0, y=0, sized to contain its frames without overlap; #841 moved it there with its node IDs unchanged. It is built from Components-page library instances on `Home — final`. Its three new components are marked DESIGN-ONLY and stay inside the proposal section rather than on Components until Jesse accepts the proposal:

- `PrimaryNavigation` `layout=rail` (`322:11874`), with `active=Home|Invest` and `collapsed=false|true` (240px and 64px)
- `PrimaryNavigationItem` (`322:11831`)
- `ShellHeader / layout=rail` (`322:11903`)

Two library components gained additive variants, and Jesse must publish the library for them to leave the file: `SignedBalanceBar` (`293:5935`) has a `width=fixed|fill` axis whose `fill` variants scale with the card, and `Icon` (`156:1726`) has `panel-left-close` and `panel-left-open` for the rail toggle. No existing variant changed. After #841, every instance in the section resolves to a Components-page library component (none to Archive, the legacy `Card` `12:28` or a deprecated part), with no nested overrides on `FinanceRow`, `ShimmerRow` or `ActivityLoader`. The rows therefore follow the reconciled rules: a lone value beside a two-line label is centred, and the loading `ShimmerRow` mark is a centred 40px circle. The proposal has no review screen, so the one-line review address does not apply. The frames are:

- `Home — desktop` (`323:11868`)
- `Invest — desktop` (`324:13214`)
- `Cash — L2 desktop` (`324:13405`)
- `Home — 1024` (`323:12150`)
- `Home — 1023 (below breakpoint)` (`323:12684`)
- the loading (`323:12428`) and Activity error (`323:12551`) states
- `Home — desktop / rail collapsed` (`367:13785`) and the `Rail collapse — interaction spec` (`395:15150`)

After Jesse's September 24 review, Home at desktop width has two columns with Activity on the right (following the #638 desktop screen `Desktop / Home / US / funded`, `311:15379`), the segmented bar spans the total card, and the rail collapses to a 64px icon rail. The `Notes — desktop rule` frame (`325:13090`) holds the proposed breakpoint, the column and scroll decisions, the rail collapse spec and the references. The Storybook journey `journeys-desktop-shell` renders the same proposal with production components. Node inventory and residual differences are in [figma-mapping.json](figma-mapping.json) `desktopProposal`. It is not approved until Jesse selects it; see [UI direction](../ui-direction.md#desktop-proposal-unaccepted).

## Comment pass

1. Jesse comments or edits the relevant Figma page.
2. Jesse comments `process Figma comments` on issue #683.
3. The worker reads comments through `GET /v1/files/ixgttt6IurKynsvMJpLYDC/comments` using a private token file outside the repo. The token is loaded only into the request environment and is never printed, captured, or committed.
4. The worker reads changed frames and component structure through the Figma MCP, compares them with the current React owner and a real browser render, and classifies each thread.
5. The worker revises the existing owned nodes in place through the Figma edit connection, or implements an approved result through the production-component Storybook/code loop. Existing published component IDs and Code Connect mappings are preserved unless Jesse explicitly authorizes a migration.
6. The worker replies to every newly processed thread through the Figma comments REST API, reports component-definition changes that need publication, refreshes screenshots, and records residual differences. Older answered threads are not duplicated. The REST comments API cannot set `resolved_at` (no endpoint; `POST`/`PUT`/`resolve` variants return 404) and the Plugin API does not expose comments, so verified-satisfied threads are reported for a single manual Figma-UI resolve action rather than closed programmatically. The exact open-and-satisfied thread IDs live in [figma-mapping.json](figma-mapping.json) `commentHygiene`.

Figma comments are product direction, not automatic code authorization. A Jesse-directed synthesis remains unapproved until Jesse approves that rendered frame. Code adoption then follows the production-component Storybook, browser-validation, and current-head evidence contracts.
