# MoneyPrimaryAmount

| Item | Code | Figma | MVP | Built on | Owner | P |
| --- | --- | --- | --- | --- | --- | --- |
| MoneyPrimaryAmount | yes | `166:1776` state=empty\|entered\|error as a native numeric input. Prefix, Figure (digits and 2px caret room) and suffix centre as one run over the available width, with 12px between the amount and available line. The caret follows entered digits and precedes the muted 0 when empty; an empty amount also mutes the prefix and suffix. `Show prefix` (on), `Prefix` (`$`), `Show suffix` (off) and `Suffix` (` USDC`) configure the run. A letter prefix such as `Rp` ends in a no-break space, because Figma drops the trailing space code keeps. `state=error` keeps the amount foreground and turns the available line into a destructive explanation; its Figma copy and icon still differ from code's `Only … available`. Since #941, `MoneyAmountDisplay` renders an `inputMode="decimal"` input, and `overAvailable` shows the destructive `Only … available` line. The unit toggle sits under the available line, and the chips sit above the CTA. Code and Figma match on alignment since [#1015](https://github.com/jessepollak/home/issues/1015), per Jesse's 2026-09-26 decision. Usage reference: `512:19943`. | yes | Input `variant="amount"` | `client/money-modal/amount.tsx` | P0 |
