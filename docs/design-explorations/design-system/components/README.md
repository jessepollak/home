# Home Figma component notes

The [gap matrix](../../design-system.md#gap-matrix) defines the columns and priorities. Frequently revised component entries live here, one file per component; add future components as files rather than growing the shared matrix. Older unchanged rows remain in the matrix.
