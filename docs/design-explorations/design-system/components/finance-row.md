# FinanceRow

| Item | Code | Figma | MVP | Built on | Owner | P |
| --- | --- | --- | --- | --- | --- | --- |
| FinanceRow | yes | `96:1147` (#683). Revision 4: every variant is ItemMedia + `finance-row-body` (lone sides vertically centred, two two-line sides title-aligned) + one 16px trailing slot. The slot holds `ItemActions` (muted `chevron-right` Icon) or a primary `rotate-cw` read-retry ghost Button outside the row activation Button. Both preserve the value column's edge and Item's 12px side padding. States board `266:4265` | yes | Item | `components/finance-rows.tsx` | P0 |
