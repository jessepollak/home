# Bug bash — September 12, 2026 (evening, deployed `main`)

Reported by Jesse from the deployed app after the cleanup program. All ten fixed on `main` (bug-bash wave, Sept 12 evening). Decisions taken: `quiet` buttons are neutral ink (blue is for primary actions only); Send covers every catalog asset, recognized-only tokens stay display-only; Morpho `state.timestamp` may lag up to 24h before a rate reads as stale.

| # | Report | Root cause (as found) | Lane |
|---|---|---|---|
| 1 | Amount input text cut off on the right (`$0` clipped) | Ticker box/mask vs `max-inline-size`/transform in the amount container | BB1 |
| 2 | Amount animation stops after the second digit | Auto-fit re-measured on its own resize and fought number-flow (fixed `2013213`); the ticker's grow-only digit reservation needs a no-reservation mode for keypad entry instead of a per-length remount | BB1 |
| 3 | Add money button offset; should be primary/blue | Secondary `Button` with `display: contents` label inside a grid; icon stacked above label | BB2 |
| 4 | Transaction details: duplicate bottom line, 88px label column wraps ("Token contract"), "View on BaseScan" ugly | `.row:last-child` border + `Divider`; fixed 88px label track; explorer link styling | BB3 |
| 5 | Dismiss X on modals is blue by default | `quiet` button variant colour is `--home-ui-color-action` | BB1 |
| 6 | Hover state on Activity rows looks wrong | `ListRow` control hover background/radius across the whole row | BB1 |
| 7 | "APY stale" consistently on Save | Freshness rule treats Morpho `state.timestamp` (lags by design) like `fetchedAt` under the 5-minute window | BB4 |
| 8 | Send asset selector shows two indicators; only ETH + USDC | `Select` renders its own chevron and the amount step passes another as `suffix`; send supports only USDC/ETH — should support every catalog holding (recognized-only tokens stay display-only) | BB4 |
| 9 | Balances show letter fallbacks instead of icons; ordering wrong | Icon source for recognized tokens is on-chain metadata (rarely present) instead of Codex image URLs; ordering not by fiat value after the cash section | BB2 |
| 10 | Activity row type hierarchy: names always bold | `ListRow` label weight is `strong` | BB1 |
